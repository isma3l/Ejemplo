<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Ejemplo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryDefault</item>
        <item name="colorPrimaryVariant">@color/primaryDefault</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primaryLight</item>
        <item name="colorSecondaryVariant">@color/primaryLighter</item>
        <item name="colorOnSecondary">@color/colorBackgroundBase</item>
        <item name="colorOnSurface">@color/primaryDefault</item>
        <item name="colorError">@color/errorDefault</item>
        <item name="colorOnError">@color/errorLight</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

</resources>